import {Route, BrowserRouter as Router, Routes} from "react-router-dom";

// added the scss file
import "./styles/app.scss"

function App() {
  return (
  <Router>
    <header/>
    <Routes>
      <Route path="/" element={} />
    </Routes>
  </Router>
  ); 
  
  
}

export default App;
